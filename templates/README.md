# EUB KiCad Templates

This directory is reserved for optional KiCad project templates.

No template is currently treated as the shared default. Add a template only
when it captures a reusable EUB board setup, such as layer stack assumptions,
DRC defaults, title block conventions, or library registration examples.

Keep experimental project setup local until it is stable enough to reuse.
